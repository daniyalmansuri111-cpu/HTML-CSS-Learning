name: Deploy All Projects

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: Noir-Fragrance/package-lock.json

      - name: Install Noiré dependencies
        working-directory: Noir-Fragrance
        run: npm ci

      - name: Build Noiré
        working-directory: Noir-Fragrance
        run: npm run build

      - name: Prepare GitHub Pages
        run: |
          mkdir pages

          rsync -av \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='Noir-Fragrance' \
            ./ pages/

          mkdir -p pages/Noir-Fragrance

          cp -r Noir-Fragrance/dist/. pages/Noir-Fragrance/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
